<div class="mini_playlists">
    <img src="{cover}" alt="{name}">
    <span class="name">{name}</span>
    <div class="playlist_action">
        <button onclick="add_track_to_playlist('{id}', $('.search_playlists_result').attr('track_id'))">Add To Playlist</button>
    </div>
</div>